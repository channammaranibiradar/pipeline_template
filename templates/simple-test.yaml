apiVersion: "templates.cloudbees.com/v1alpha1"
kind: "Template"
metadata:
  name: "simple-test"
spec:
  description: "Simple test pipeline"
  template: |
    pipeline {
      agent any
      stages {
        stage('Hello') {
          steps {
            echo '✅ Simple pipeline is working!'
          }
        }
      }
    }
