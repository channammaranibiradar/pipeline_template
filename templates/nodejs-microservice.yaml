apiVersion: "templates.cloudbees.com/v1alpha1"
kind: "Template"
metadata:
  name: "nodejs-microservice"
spec:
  description: "Node.js microservice pipeline"
  template: |
    pipeline {
      agent any
      stages {
        stage('Checkout') {
          steps {
            echo "Checking out code..."
          }
        }
        stage('Build & Test') {
          steps {
            echo "Building and testing Node.js microservice..."
          }
        }
        stage('Deploy') {
          steps {
            echo "Deploying microservice..."
          }
        }
      }
    }
